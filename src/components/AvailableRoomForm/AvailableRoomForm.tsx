export interface IAvalableRoomFormProps {
  onRoomStateSubmit: (premium: number, economy: number) => void
};

export const AvailableRoomForm = (props: IAvalableRoomFormProps) => {
  return <section aria-label="available-rooms-form">

  </section>
};
